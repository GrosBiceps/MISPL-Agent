# MISPL Agent v3 — Dépendances Python
# Installer : pip install -r requirements.txt

# ── RAG & Vectorstore ──────────────────────────────────────────────────────────
chromadb==1.5.9
rank-bm25==0.2.2                        # BM25 pour recherche hybride
sentence-transformers==5.6.0            # Embeddings locaux offline
PyYAML==6.0.3                           # Parsing frontmatter YAML des fiches KB
nltk==3.9.4                             # Stemmer SnowballStemmer("french") pour BM25

# ── LLM via OpenRouter (API compatible OpenAI) ────────────────────────────────
openai==2.44.0                          # Client OpenAI — compatible OpenRouter

# ── Interface ─────────────────────────────────────────────────────────────────
streamlit==1.58.0                       # Interface web

# ── Utilitaires ───────────────────────────────────────────────────────────────
python-dotenv==1.2.2                    # Chargement .env
tqdm==4.68.3                            # Progress bar ingestion

# ── Monitoring ressources (CPU/RAM/disque/réseau/GPU → rapport Plotly) ────────
psutil==7.2.2                           # Métriques CPU/RAM/disque/réseau
# GPUtil n'est plus maintenu depuis 2020 (dernier commit archivé) — envisager
# nvidia-ml-py comme alternative à évaluer plus tard (hors périmètre de ce pin)
GPUtil==1.4.0                           # Charge & VRAM GPU NVIDIA
plotly==6.8.0                           # Rapport HTML autonome

# ── Dev & Tests ───────────────────────────────────────────────────────────────
pytest==9.1.1
pytest-cov==7.1.0

# ── API & Authentification (comptes, sessions) ────────────────────────────────
fastapi==0.141.1
uvicorn[standard]==0.49.0
sqlalchemy==2.0.52
argon2-cffi==25.1.0
email-validator==2.3.0                   # requis par pydantic.EmailStr
httpx==0.28.1                             # requis par fastapi.testclient.TestClient

# ── Outil de contrôle PI (optionnel) — tools/check_ip_similarity.py ──────────
# numpy et scikit-learn sont déjà installés comme dépendances de
# sentence-transformers ; ils sont épinglés ici aux mêmes versions.
numpy==2.5.0                            # matrices n-grammes / similarités
scikit-learn==1.9.0                     # TF-IDF + similarité cosinus
beautifulsoup4==4.15.0                  # extraction du texte de l'aide HTML
pypdf==6.19.0                           # optionnel : lecture des PDF du manuel
